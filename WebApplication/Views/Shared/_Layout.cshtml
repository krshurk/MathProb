<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - MathProb</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/WebApplication.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-light bg-white">
            <div class="container-fluid d-flex justify-content-between align-items-center" style="gap: 24px;">
                <span class="navbar-brand mb-0 h1" style="color: #fff;">MathProb</span>
                <div class="d-flex" style="gap: 16px;">
                    <a class="nav-link text-link" asp-area="" asp-controller="Home" asp-action="Index">Главная</a>
                    <a class="nav-link text-link" asp-area="" asp-controller="Material" asp-action="Theory">Теория</a>
                    <a class="nav-link text-link" asp-area="" asp-controller="Tasks" asp-action="Index">Задачи</a>
                    <a class="nav-link text-link" asp-area="" asp-controller="Tasks" asp-action="Exam">Экзамен</a>
                </div>
                <div class="d-inline-flex" style="gap: 10px;">
                    @if (User.Identity.IsAuthenticated)
                    {
                        <a class="nav-link text-link" asp-area="" asp-controller="Profile" asp-action="Index">Профиль</a>
                        <form asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
                            <button type="submit" class="text-link nav-link">Выйти</button>
                        </form>
                    }
                    else
                    {
                        <a class="nav-link text-link" asp-area="" asp-controller="Account" asp-action="Login">Войти</a>
                        <a class="nav-link text-link" asp-area="" asp-controller="Account" asp-action="Register">Регистрация</a>
                    }
                </div>
            </div>
        </nav>
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="footer mt-auto">
        <div class="footer-container container">
            <div class="footer-section">
                <h5>О нас</h5>
                <p>MathProb — сервис для подготовки по теории вероятностей. Учимся решать задачи, разбираем теорию и готовимся к экзаменам по терверу!</p>
            </div>
            <div class="footer-section">
                <h5>Контакты</h5>
                <p>Email: <a href="mailto:info@mathprob.ru">info@mathprob.ru</a><br>Тел: +7 (999) 123-45-67</p>
            </div>
        </div>
        <div class="footer-bottom mt-3">
            &copy; 2024 MathProb. Все права защищены.
        </div>
    </footer>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html> 